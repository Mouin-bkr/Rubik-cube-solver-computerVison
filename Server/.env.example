# Server environment variables
# Override Python interpreter if not using local .venv
PYTHON_BIN=/usr/bin/python3
# Flask dynamic port (Railway sets PORT automatically; leave blank locally)
PORT=5000
# Enable debug locally only
FLASK_DEBUG=true
# Allowed CORS origin for Flask
CORS_ORIGIN=http://localhost:5173
